package com.project.miniproject.teams.repository;

import com.project.miniproject.teams.entity.OnProject;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OnProjectRepository extends JpaRepository<OnProject, Integer> {
}
