package com.project.miniproject.projectphases.repository;
import com.project.miniproject.projectphases.entity.PhaseCatalog;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PhaseCatalogRepository extends JpaRepository<PhaseCatalog, Integer> {
}
